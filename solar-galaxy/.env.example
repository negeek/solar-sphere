# For local `go run .` dev. Copy to solar-galaxy/.env — it's loaded
# automatically if present (see main.go); Docker doesn't need this file.
AUTH_BASE_URL=http://localhost:3000
SENTINEL_BASE_URL=http://localhost:5000
PORT=8080
LOG_LEVEL=debug
LOG_FORMAT=text

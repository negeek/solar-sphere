# For local `go run .` dev. Copy to solar-auth/.env — it's loaded
# automatically if present (see main.go); Docker doesn't need this file.
DATABASE_URL=mongodb://localhost:27017
DB_NAME=solarsphere
SIGNING_KEY=
VERIFICATION_KEY=
PORT=3000
LOG_LEVEL=debug
LOG_FORMAT=text

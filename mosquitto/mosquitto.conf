# Minimal self-host config: anonymous access on the plain MQTT port.
# Fine for a private/personal deployment behind your own network; if you're
# exposing this broker publicly, add authentication (password_file) and TLS
# before doing so.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
